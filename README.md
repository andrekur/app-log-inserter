# HW CLOUD N2 app-log-inserter
## Установка
```
git clone https://github.com/andrekur/app-log-inserter.git
cd app-log-inserter/_CD
mv env.example .env
sudo docker-compose build
sudo docker-compose up
```
после проверьте адрес localhost:80/docs